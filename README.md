# Ajay Residency website

Upload the contents of this folder to a GitHub repository and enable GitHub Pages from the repository root. The home page is `/` and the rooms page is `/rooms/`; neither URL needs `.html`.
